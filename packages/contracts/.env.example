# Deployer key. Testnet only in this file; never commit a mainnet key.
PRIVATE_KEY=0x

# Address receiving DEFAULT_ADMIN_ROLE and PAUSER_ROLE.
# Deliberately required — it does not default to the deployer.
ADMIN=0x
